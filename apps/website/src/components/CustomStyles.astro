<style is:inline>
  /* AstroWind `--aw-*` theme tokens and fonts are generated from the brand
     semantic contract; see libs/brand/generated/website-theme.css (imported
     via assets/styles/tailwind.css). Only presentational extras live here. */
  ::selection {
    background-color: color-mix(in srgb, var(--granville-color-marketing-accent) 40%, transparent);
    color: var(--granville-color-slate-50);
  }
</style>
