Archived legacy draft files for the branded domain.

Files:

- `index.html` is the old standalone static homepage draft
- `styles.css` is the companion stylesheet for that draft

Notes:

- `../branded-domain-site/` is the active public-facing implementation
- keep these files for historical reference only
